git config --global user.name "名字"
git config --global user.email "電子郵箱"
git init 轉換成有版本管理功能Git儲存庫
clear 清空terminal的內容


git remote add origin https://github.com/Renee820/StudyGit.git 鏈接本地和雲端的儲存庫

git branch -M main 
把分支master改為中性的main

git push -u origin main
把本地main分支的資料推送至雲端